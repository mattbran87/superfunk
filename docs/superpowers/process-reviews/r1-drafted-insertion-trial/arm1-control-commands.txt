---
grep -c "Never truncate an identifier" docs/code-standards.md; grep -c "Error-Message Copy" docs/code-standards.md; tail -c 200 docs/code-standards.md | od -c | tail -5
---
cp docs/code-standards.md /tmp/cs-orig.md && cat >> docs/code-standards.md <<'EOF'

---

## Error-Message Copy

Error messages must name the failing input and the expected
format in the same sentence, so a user can correct the mistake
without reading source code. Log lines above WARN must carry the
request identifier. Never truncate an identifier in user-facing
output.
EOF
git diff --stat docs/code-standards.md && diff <(tail -n 7 docs/code-standards.md) <(printf '%s\n' '## Error-Message Copy' '' 'Error messages must name the failing input and the expected' 'format in the same sentence, so a user can correct the mistake' 'without reading source code. Log lines above WARN must carry the' 'request identifier. Never truncate an identifier in user-facing' 'output.') && echo TAIL-PASS; cp /tmp/cs-orig.md docs/code-standards.md && git diff --stat docs/code-standards.md; echo "restored: $?"
