# Process Review — Running Notes

Append-only log. Each entry marks one finding a review catches on its
first pass (spec-compliance, code-quality, or the final whole-branch
review), tagged `Catch`. `process-review` reads this log,
cross-references `git log`, and may surface `Miss`, `Friction`, or
`Gap` patterns across entries when it synthesizes a review file.

Format: `- <YYYY-MM-DD> | Catch | <task/spec label> | <check that caught it> | <one-line finding>`

The fourth field names the specific check that produced the Catch — for
example `writing-plans item 10`, `brainstorming item 6`, or
`SDD spec-review`. When no check produced it, write exactly
`none — found ad hoc`. That value records a real fact and keeps
attribution honest: a format offering no way to say "nothing caught
this" pressures a writer to invent one.

Entries below dated before 2026-09-01 carry four fields, without the
check field. They stay as written — reconstructing attribution from
memory would fabricate it.

<!-- entries below this line -->
- 2026-08-19 | Catch | Task 2 (process-review) | Process-review skill's Step 4 dropped the design spec's required Specs Reviewed section, and Self-Review only checked trigger presence, not format/reason quality (git log 437d959, missed by the running log since it predates this mechanism's own existence)
- 2026-08-19 | Catch | Final review (process-review) | Design spec's Falsifiable Criteria and the plan both retained a stale "five sections" count after the Decision section was corrected to six (git log 04fbdcf, missed by the running log since it predates this mechanism's own existence)
- 2026-08-20 | Catch | Task 1 (pseudocode-during-planning) | Pseudocode section had no worked example, only abstract rules
- 2026-08-20 | Catch | Task 1 (pseudocode-during-planning) | Self-Review's Pseudocode check verified trigger presence only, not format or reason quality
- 2026-08-20 | Catch | Task 2 (pseudocode-during-planning) | Pseudocode context dispatch bullet gave no method for matching a task to its triggers
- 2026-08-20 | Catch | Task 2 (pseudocode-during-planning) | Pseudocode context bullet lacked the why-explanation and visibility requirement its Directory context sibling has
- 2026-08-20 | Catch | Final review (pseudocode-during-planning) | Multi-task-same-trigger attribution undefined -- a dispatch could fold another task's pseudocode into the wrong task's context
- 2026-08-20 | Catch | Final review (pseudocode-during-planning) | Both Task 1 and Task 2 needed a fix round for similar gaps (missing concrete guidance, missing parity with sibling content) traced to the plan's own drafted text, not implementer error -- future plans handing an implementer verbatim skill-file edits should self-check against the target file's own sibling conventions during Self-Review
- 2026-08-20 | Catch | Task 2 (lessons-and-patterns) | Promotion-rule bullet dropped "that applies across many future situations" from the design spec's exact phrasing
- 2026-08-20 | Catch | Task 3 (lessons-and-patterns) | <spec-slug> had no fallback for the no-design-spec Finish branch
- 2026-08-20 | Catch | Task 3 (lessons-and-patterns) | Promotion-rule framing drifted from code-standards.md's independent-OR structure
- 2026-08-20 | Catch | Task 4 (lessons-and-patterns) | Lessons/patterns reading bullet had no visibility clause, unlike its .context.md sibling
- 2026-08-20 | Catch | Final review (lessons-and-patterns) | Three fix rounds traced to the plan restating the same design-spec rule in two different tasks without cross-checking the restatements against each other
- 2026-08-20 | Catch | Task 1 (checklist-construction) | Wording redundancy: "not once per split sub-checklist" repeated "split" already established one clause earlier
- 2026-08-20 | Catch | Task 2 (checklist-construction) | Per-section instruction said "phase" but two of six groups (Quality Checks, Deployment) don't say "Phase" in their own heading
- 2026-08-20 | Catch | Task 1, 2, 3 (checklist-construction) | Three separate code-quality findings misapplied a stated rule literally without checking source intent or existing shipped precedent first -- all three overridden after independent verification (the 5-9 cap read as a floor, a tagging comparison used a mismatched sibling, a "needs examples" finding ignored two already-shipped instances of the same open-ended pattern)
- 2026-08-20 | Catch | Final review (checklist-construction) | Falsifiable Criterion 2's literal grep pattern didn't match test-driven-development's past-tense "Checked" wording -- a false failure if re-run literally
- 2026-08-20 | Catch | Final review (checklist-construction) | The shipped "5-9 items" rule text itself reads ambiguously as a floor, the same ambiguity that caused a reviewer override earlier in this same sub-project
- 2026-08-21 | Catch | Task 3 (hazard-signal-words) | "Before writing any code" inaccurately covered commit-time conventions too, timing mismatch
- 2026-08-21 | Catch | Task 4 (hazard-signal-words) | scripts/review-package's git log --oneline truncates to subject lines, making the new severity-trailer check impossible to actually perform
- 2026-08-21 | Catch | Task 5 (hazard-signal-words) | File Naming bullet conflated the Feature Directories rule with a general "dated artifacts" claim and dropped the short-and-descriptive preference entirely
- 2026-08-21 | Catch | Task 6 (hazard-signal-words) | Acceptance-criteria parenthetical was terse enough to misread as one shared bar for two textually distinct validation tracks
- 2026-08-21 | Catch | Task 7 (hazard-signal-words) | First live-trial attempt was invalid -- scratch fixture never had the real convention docs copied in, so the AI fell back to generic conventions instead of testing the actual wiring
- 2026-08-21 | Catch | Task 6 (hazard-signal-words) | A code-quality finding claiming the template rule "wasn't mentioned" was factually wrong -- the shipped bullet named it explicitly; overridden after independent verification, not fixed
- 2026-08-25 | Catch | Task 1 (concept-index) | Step 3's unattended, automatically-triggered path had no hand-edit check before overwriting or removing an existing row, unlike project-definition's Step 6, which folds the equivalent check directly into its own numbered update step
- 2026-08-25 | Catch | Task 1 (concept-index) | Concept Units' Feature bullet specified what goes in the Description column but never specified what goes in the Concept column, unlike the Skill and Directory bullets
- 2026-08-25 | Catch | Task 1 (concept-index) | Round-1 fix's cross-reference text incorrectly attributed an "interactive run" to Step 2, which structurally can never face an existing row to overwrite (Step 1 routes to Step 2 only when no index exists yet)
- 2026-08-25 | Catch | Task 1 (concept-index) | Round-2 fix's replacement text invented a "human invokes concept-index's Step 3 directly" scenario, contradicted by Step 3's own unedited "never run this step standalone" sentence -- required a third fix round removing the invented distinction rather than rephrasing it
- 2026-08-25 | Catch | Task 2 (concept-index) | Commit message cited the wrong Lesson slug (per-task-outcome-capture, a real but unrelated Lesson about a plugin-name collision) instead of the actual matching Lesson filed under review-recommendations-followup
- 2026-08-25 | Catch | Task 2 (concept-index) | New Finish-step paragraph had no why-clause, unlike every sibling paragraph in the same section -- same class of gap task-reviewer-prompt.md's Minor #4 caught earlier this session
- 2026-08-25 | Catch | Task 3 (concept-index) | Concept-index context's negative case dropped the "note which X you checked, so the check stays visible instead of quietly not happening" clause both sibling bullets (Directory context, Pseudocode context) carry -- traces to the plan's own text, not an execution defect; Minor, deferred to final review
- 2026-08-25 | Catch | Final review (concept-index) | Concept-index context bullet's missing visibility clause (deferred from Task 3) confirmed as Important, not Minor, once viewed against the spec's own stated silent-skip rationale
- 2026-08-25 | Catch | Final review (concept-index) | Hand-editing section's "confirming first" described a confirmation mechanism the shipped design never implements (the real mechanism is skip-and-flag, no human confirmation loop)
- 2026-08-25 | Catch | Final review (concept-index) | Step 3's hand-edit detection compared a row's Description against a fresh re-derivation, but Step 2's Skill-description trim is a judgment call, not deterministic -- the comparison could not reliably distinguish a real hand-edit from ordinary re-derivation drift, and could not detect row reordering at all
- 2026-08-25 | Catch | Final review (concept-index) | Falsifiable Criterion 2's "same commit pattern as tracker/lessons-learned" claim contradicted the shipped skill's own always-separate-commit rule; Criterion 3's skip trial coached the agent by naming the exact trigger paragraph, showing followability rather than genuine trigger discrimination
- 2026-08-25 | Catch | Final review (concept-index) | Fix wave's own correction to the hand-edit check overshot into an unconditional human-decision gate on every rename/delete, contradicting Falsifiable Criterion 4 and Task 5's own already-passed live trial -- caught by a second re-review round, not the first
- 2026-08-25 | Catch | Final review (concept-index) | The corrected hand-edit check's "re-read the unit's current underlying source" was unsatisfiable for a deleted unit (no source left to compare), and Step 1 routed a direct human invocation into Step 3, which Step 3's own header and the Hand-editing section both declare impossible -- both caught by a third confirming re-review, fixed directly by the controller rather than another dispatch round
- 2026-08-25 | Catch | Final review (concept-index) | The controller (coordinator) missed its own three-day-old outcomes-bookkeeping mechanism a second time in this same sub-project, despite an explicit start-of-session reminder naming the exact prior failure -- promoted to a Pattern as a genuine recurrence, not a first instance
- 2026-08-25 | Catch | Final review (concept-index) | Controller's own `git commit --amend` attached a copy-pasted message from a different commit to the wrong diff -- self-caught immediately via a direct diff comparison against the intended content, before the mismatch could ship
- 2026-08-26 | Catch | Task 3 (process-review-recommendations-batch-2) | A new Pattern's Originating-lessons citation had no matching entry in lessons-learned.md, breaking this project's 8-for-8 citation convention -- the rule's real origin was a process-review Recommendation, never captured as a Lesson at the time
- 2026-08-26 | Catch | Task 4 (process-review-recommendations-batch-2) | Task 1's own just-shipped `<plan-slug>` placeholder was never defined precisely; a live trial reasonably used the full dated filename instead of the short slug every real historical entry actually uses, surfacing the ambiguity before it could recur
- 2026-08-26 | Catch | Final review (process-review-recommendations-batch-2) | The shipped notes.md gate was reminder-shaped (no grep command, no explicit commit step) despite the originating Recommendation explicitly asking for "not a restated reminder" -- confirmed by this sub-project's own commit timestamps, which showed Task 3's fix-round entry landing 8 minutes after Task 3 was marked complete, batched with Task 4's finding
- 2026-08-26 | Catch | Final review (process-review-recommendations-batch-2) | Both closed Recommendation checkboxes in the prior process review remained unchecked despite being shipped, against an established 2-for-2 project convention
- 2026-08-26 | Catch | Final review (process-review-recommendations-batch-2) | Falsifiable Criterion 4 claimed the new Pattern rule's worked example "cites both real recurrences," but the shipped design deliberately splits one recurrence per rule's own example -- and after the criterion got corrected, the design spec's own Decision bullet still asserted the false original claim, contradicting the criterion in the same document
- 2026-08-26 | Catch | Final review (process-review-recommendations-batch-2) | Task 4's own live-trial prompts violated both rules of the very A/B-testing Pattern this sub-project shipped one task earlier (named the exact mechanism under test, handed the agent the finding, used the literal disqualifying phrase "read fresh from disk") -- a plan defect authored before the Pattern existed, not an execution error
- 2026-08-27 | Catch | Final review (cross-section-mechanism-consistency) | re-review-prompt.md's own Scope section stayed unqualified after the carve-out shipped, contradicting the exception added lower in the same file -- the exact failure shape this sub-project exists to catch, found in its own shipped edit
- 2026-08-27 | Catch | Final review (cross-section-mechanism-consistency) | subagent-driven-development/SKILL.md's "flags new breakage in the fix diff only" went stale once the carve-out shipped in a sibling file -- item 8's own grep scope (same file plus design spec only) would not have caught this instance, a real gap in the check's own wording
- 2026-08-27 | Catch | Final review (cross-section-mechanism-consistency) | Item 8 narrowed Recommendation 3's literal wording ("name every other section") to a grep-and-confirm step producing no written artifact, and the design spec never documented the narrowing at Decision time
- 2026-08-27 | Catch | Final review (cross-section-mechanism-consistency) | Both live trials exercised only a positive, single-file, two-branch case -- the design-spec clause, the negative (correctly-does-not-fire) case, and a mechanism spanning more than two sections all went untested
- 2026-08-27 | Catch | Final review (cross-section-mechanism-consistency) | Recommendation 3's checkbox, the design spec's Status line, and tracker.md's shipped-specs list all remained unactioned after implementation completed, pending this Finish step
- 2026-08-27 | Catch | Re-review of fix wave (cross-section-mechanism-consistency) | The first fix wave (commit 99ed48c) reconciled one unqualified diff-only statement per file but left a second one standing in each of the same two files (SKILL.md's Red Flags table, re-review-prompt.md's own opening framing sentence) -- the same recurring shape, caught by the scoped re-review rather than a fresh pass
- 2026-08-27 | Catch | Task 1 (cross-section-sibling-scope) | A code-quality review flagged item 8's widened sentence (46 words) and its "if ... names one" construction as new STE violations; both already existed verbatim in already-shipped, already-approved precedent (item 8's original 32-word sentence, the carve-out's identical "if the plan's Goal line ... names one" construction) -- overridden per verify-against-precedent-before-flagging rather than fixed, since fixing only this instance would also break parity with Task 2's identical, unedited clause
- 2026-08-27 | Catch | Final review (cross-section-sibling-scope) | The widened clause never states whether "every other file in the same directory" recurses into subdirectories -- four skill directories have one, including subagent-driven-development/scripts/, the directory this whole fix originated from; no live contradiction exists today, but the ambiguity went unrecorded
- 2026-08-27 | Catch | Final review (cross-section-sibling-scope) | The design spec never recorded why subagent-driven-development/SKILL.md's two abstract carve-out summaries and re-review-prompt.md's own Scope pointer needed no edit this time, unlike the prior sub-project where the equivalent abstraction did need one -- left a future reader with an unexplained difference
- 2026-08-27 | Catch | Final review (cross-section-sibling-scope) | The prior spec's item-8 visibility gap (no written artifact confirming the check ran) never carried forward into this spec's Deferred section, even though this sub-project widened what item 8 silently checks
- 2026-08-27 | Catch | Final review (cross-section-sibling-scope) | The negative (correctly-does-not-fire) case has now gone untested across two consecutive sub-projects in this line, with no escalation noting the streak
- 2026-08-27 | Catch | Spec self-review (cross-section-negative-case-trials) | Falsifiable Criterion 3, as originally drafted, would have forbidden naming the mechanism under test at all ("item 8," "the carve-out") in either trial's dispatch prompt -- a misreading of Rule 2, which only forbids revealing the discriminating fact or the answer, not naming which check to run; caught before the implementation plan got built around the over-strict wording
- 2026-08-27 | Catch | Finish (cross-section-negative-case-trials) | The spec's own Context section never cited docs/superpowers/process-reviews/review-after-2026-08-27-cross-section-sibling-scope-design.md, even though this spec closes that review's second Recommendation -- citing only the two intermediate specs that had deferred the gap instead; caught by re-reading the spec once more before running Finish
- 2026-08-27 | Catch | Task 2 (notes-md-direct-mode-gate) | The plan's Find block anchored the backfill edit to an entry mislabeled "(cross-section-recursion-boundary)"; the real notes.md entry at that point in the file reads "(cross-section-sibling-scope)" -- caught immediately when the Edit tool reported no match, fixed by re-reading the file's actual tail before retrying, first real exercise of the notes.md gate's own newly-widened direct-implementation trigger
- 2026-08-27 | Catch | Final review (bug-tracking) | subagent-driven-development/SKILL.md's Example Workflow never depicts any of Finish's bookkeeping steps (Status flip, tracker, Recommendation-checkbox, notes.md gate, Lessons-learned, concept-index, and now bug-tracking) -- pre-existing across five prior Finish additions, not introduced by this sub-project; documented in the design spec's Deferred section rather than patched in isolation, since a proper fix means refreshing the whole worked example in one pass
- 2026-08-27 | Catch | Task 2 (refresh-example-workflow) | The commit message for item 9 misattributed the originating final review to "cross-section-mechanism-consistency" instead of the actual source, "bug-tracking" -- caught immediately on self-review before pushing, fixed via git commit --amend on the still-unpushed commit
- 2026-08-27 | Catch | Final review (refresh-example-workflow) | subagent-driven-development/SKILL.md's process diagram (the dot digraph near the top of the file) also stops at workspace deletion, never depicting any of Finish's bookkeeping steps -- the same staleness shape this sub-project fixed in the prose Example Workflow, in a third location within the same file; left out of this sub-project's scope (a different kind of illustration than the one named in its own Decision) and documented rather than fixed here
- 2026-08-28 | Catch | Plan self-review (documentation) | writing-plans/SKILL.md's required Plan Document Header template has always included a "## Global Constraints" section (present since the original upstream import, commit 8e1262a3, predating this fork) -- every plan written this entire session omitted it, an undetected systematic miss since writing-plans' own 9-item Self-Review has no item checking a plan's own header against its own required template; caught only by reading the header template directly while reviewing this plan's Pseudocode section for an unrelated reason
- 2026-08-28 | Catch | Task 1 (documentation) | The plan's Step 8 predicted "9/9" tests passing but the correct count is 10 (a miscount when the end-to-end tests got added after the plan's Step 5-6 git-fixture test) -- caught by actually running the tests and reading the real output rather than assuming the plan's prediction, not a defect in the shipped code
- 2026-08-28 | Catch | Plan self-review (superfunk-rebrand) | Approved spec estimated "135 occurrences across 33 files" for the superpowers: prefix rewrite and said "grep-and-replace across the whole plugin/ tree" -- the actual sweep found 116 occurrences across 29 files, roughly half in upstream's own historical changelog (RELEASE-NOTES.md) and archived pre-fork design docs, which a literal whole-tree rewrite would have falsified; caught by running the real grep before building the plan's file list, not by trusting the spec's estimate, and the spec was amended and re-approved (scoped to live/instructional content only) before planning continued
- 2026-08-28 | Catch | Task 4 (superfunk-rebrand) | Plan's verification steps predicted "33" for `grep -rn ... \| wc -l` and `grep -rc ... \| awk sum`, but both commands count matching lines, not raw pattern occurrences -- one line (writing-plans/SKILL.md:110) carries two superpowers: references, so 33 total occurrences land on only 32 matching lines; caught by actually running the command before editing rather than trusting the plan's predicted count, plan updated in place before proceeding
- 2026-08-28 | Catch | Task 6 (superfunk-rebrand) | The plan predicted check_docs.py would report ALREADY_UPDATED once Task 5 modified plugin/README.md, but its first-ever real (non-fixture) invocation instead returned ACTION_NEEDED and separately crashed with a UnicodeEncodeError -- two real, previously undetected defects in code shipped earlier this session: an exact bare-filename match that never matches a nested README.md path, and a Windows stdout encoding that can't handle em dashes/arrows nearly every spec in this project uses; filed as BUG-0001 and BUG-0002 (Critical) rather than fixed inline, per user direction, since fixing a previously-shipped tool sits outside this sub-project's own scope
- 2026-08-28 | Catch | Plan self-review (process-review-recommendations-batch-3) | Both the approved spec's Falsifiable Criterion 4 and this plan's Task 3/Task 4 first drafted a `grep -c "Finish:"` prediction of 10, assuming each of the two new diagram node names contributes exactly one matching line -- testing the exact replacement text against a full simulated copy of the real file showed each node name appears on multiple lines (its own declaration plus every edge naming it), contributing 5 lines rather than 2, for a corrected total of 13; caught by actually running the substitution before finalizing either document, the same numeric-verification discipline this very sub-project ships as writing-plans' new Self-Review item 10
- 2026-08-28 | Catch | Task 4 (process-review-recommendations-batch-3) | Task 4's own verification step used `grep -n "digraph process" -A 60 ... | grep "Finish:"`, expecting 5 matches, but returned only 2 -- the digraph process block spans 65 lines (48-113) after Task 3's edit, so the 60-line window cut off before reaching the new nodes near the block's end; caught by running the actual command rather than trusting the assumed window size, corrected to -A 65 and re-verified at 5 -- a third numeric-verification miss within this same sub-project, all three caught by execution rather than by writing-plans' not-yet-shipped-when-drafted item 10 (this plan itself is what ships it)
- 2026-08-30 | Catch | Plan self-review (doc-timing-and-mutation-check) | Task 1 Step 4 predicted `grep -c "User-Facing Documentation Timing"` would return 2, reasoning that item 12's own text would repeat the capitalized section-name phrase -- checking the actual inserted text showed item 12 uses "User-facing documentation timing" (lowercase "facing"), a distinct case-sensitive string that never matches the capitalized pattern, so the real count is 1; caught by grepping the exact drafted insertion text before finalizing rather than trusting a plausible-sounding justification, applying writing-plans' own item 10 to the very plan that also amends item 10's neighbor (item 12)
- 2026-08-30 | Catch | Task 3 (doc-timing-and-mutation-check) | Task 3 Step 4's verification used `grep -c "^## Mutation Check\|^### Mutation Check"` against task-reviewer-prompt.md and returned 0, not the predicted 2 -- this file wraps its entire template in an indented code fence (4 spaces), so its real headings never sit at column 0 and an anchored `^##`/`^###` pattern can never match anywhere in the file, a class of file this project hadn't hit with an anchored heading check before; caught immediately by running the command rather than assuming success, fixed by dropping the anchor (verified count 2 unanchored), and the same anchoring bug found and fixed in Task 3 Step 1's and Task 4 Step 4's checks before they ran, since all three targeted the same fenced file
- 2026-08-30 | Catch | Spec self-review (rebrand-string-and-worktree-ignore) | Draft Falsifiable Criterion 1 claimed fixing session-start's line 27 would leave "no remaining superpowers string anywhere in the file," but a real grep found 6 total occurrences, not 2 -- three (lines 10, 11, 26) correctly reference the unrenamed using-superpowers skill directory per the original rebrand's own scope boundary, one (line 37) links to an upstream GitHub issue and should stay, and one (line 2, a plugin-identity comment) needed fixing but the draft never named it; caught by running the numeric-claim-verification check the newly-shipped brainstorming Self-Review item 6 requires, before committing the spec
- 2026-08-30 | Catch | Plan self-review (rebrand-string-and-worktree-ignore) | Task 2 Step 1's verification predicted `grep -n "creates phantom state ... \|Only proceed to Step 1b"` would return two matches, but the phrase "creates phantom state your harness can't see or manage" also appears a third time in the same file's Common Rationalizations table -- the actual count is 3; the Edit tool's own old_string block (the combined two-paragraph text) stayed unique regardless, but the plan's standalone verification prediction was wrong, caught by running the command before finalizing rather than assuming the phrase appears only once
- 2026-08-30 | Catch | Task 1 (rebrand-string-and-worktree-ignore) | The plan's Step 4 predicted `grep -c "superpowers"` on session-start would drop from 6 to 4 after the fix, but the actual count only dropped to 5 -- the retained `using-superpowers` skill name is itself a substring match for "superpowers," including inside the now-correctly-fixed line 27's own `superfunk:using-superpowers`, so a bare substring count could never reach a clean number this fix controls; caught by running the command against the real edited file, root-caused (not just re-guessed), and both the spec's Criterion 1 and the plan's Task 1/Task 3 checks corrected to verify the specific bad strings' absence instead of counting a substring that legitimately recurs elsewhere -- the actual shipped fix (verified via the corrected check) was already correct throughout
- 2026-08-30 | Catch | Spec self-review (pattern-template-and-convention-bootstrap) | Wrote most of a design spec for D2/D3 (ship pattern-template.md and ai-code-guidelines.md into the plugin) before checking a tangential numeric claim ("25+ commits") against the source trial report -- the report's own text said "35+", and re-reading the whole document to find the real number surfaced a correction embedded in the same file, dated the same day, retracting the original D2/D3 finding entirely ("do not exist anywhere" and "invented a format" were both wrong -- the files exist in this dev repo, just outside plugin/, and nothing got improvised). The spec got fully rewritten around the corrected finding's own recommended fix (inline the pattern structure, guard the doc reads) instead of shipping copies into the plugin; caught before committing, not after, but only by accident of checking an unrelated number -- a source document already read once still needs a fresh read before building a second artifact from it, since a correction can land in the same file after the first read
- 2026-08-30 | Catch | Task 1 (pattern-template-and-convention-bootstrap) | Task 1 Step 4's second check predicted `grep -rc "pattern-template.md" plugin/ docs/` would return 0 everywhere, but historical specs, plans, and notes.md entries legitimately still mention the filename as a record of what happened -- the same "don't rewrite history" precedent this project already applies elsewhere. Caught by running the original (too-broad) check before considering the task done; corrected the plan in place to scope the check to `plugin/` only, where it correctly returned zero
- 2026-08-30 | Catch | Task 4 live trial (pattern-template-and-convention-bootstrap) | The scaffold-accepted trial surfaced a real but minor behavioral gap beyond what the plan anticipated: when the initial brainstorming prompt didn't state a language upfront, the session correctly identified the missing-CLAUDE.md/ai-code-guidelines.md condition but chose to defer the offer until the language became known, then did not proactively bring the offer back on its own the next turn -- it only resurfaced after an explicit prompt asking what happened to it. Once resurfaced it worked correctly (skipped the now-redundant language question, drafted genuinely substantive content, committed before resuming). Not a spec violation (the bullet doesn't mandate immediate timing) and not filed as a new bug given single-occurrence and minor severity; recorded for visibility in case it recurs
- 2026-08-30 | Catch | Spec self-review (fix-wave-regression-amendment) | Two "direct quotes" in the draft Context section, attributed to the trial's own internal process-review file, turned out to be paraphrases reconstructed from memory rather than the file's actual text -- one had "cap"/"stop" where the real text says "rule"/"prevent"; the other ("The distinction the skill is missing is between...") didn't exist verbatim anywhere in the file at all, fabricated from the general shape of a nearby Recommendation. Caught by grepping the actual source file for the quoted phrases before finalizing and finding no match, then reading the real surrounding text to fix both quotes -- the exact "stale source, quoted from memory" risk the previous sub-project's Lesson explicitly flagged as "revisit if it recurs," recurring in the very next sub-project
- 2026-08-30 | Catch | Plan self-review (equality-not-containment-note) | Task 1 Step 3's verification predicted `grep -c "containment"` would return 1, but the plan's own drafted insertion text uses the word "containment" twice ("substring containment" and "since containment can stay true") -- caught by counting the actual drafted text before finalizing rather than assuming a word appears once; corrected to 2 and confirmed against a real-line simulation before committing
- 2026-08-31 | Catch | Plan self-review (checkpoint-priority-and-conditional-gate) | Two verification commands (Task 2 Step 5, Task 3 Step 1) anchored on the phrase "own body produces exactly this kind" -- but that phrase lives in this spec's own Context section describing the D5 contributing defect, not in the actual skill text the plan inserts into brainstorming/SKILL.md. Both checks would have returned 0 against the real file regardless of whether the edit landed correctly, a false-negative risk. Caught by test-running the grep against the real drafted insertion text (not the spec's descriptive prose) before finalizing; corrected both to "overstates or understates its own list," verified present in the actual inserted sentence and absent (0) from the file's current baseline. A third command (Task 3 Step 1's gate-wording check) separately caught its own anchor collision: "unless the" already exists once elsewhere in the file (the Visual Companion section's "unless they raise it"), corrected to a more specific unique phrase before finalizing
- 2026-08-31 | Catch | Task 2 (checkpoint-priority-and-conditional-gate) | The plan's own fix for the previous catch, `grep -c "overstates or understates its own list"`, still returned 0 against the real post-edit file -- the phrase spans a markdown line wrap (line 172 ends "...overstates or understates its", line 173 begins "own list creates..."), so no single-line grep can match the full phrase regardless of content correctness. The inserted text itself was already correct and unchanged. Caught by running the command against the real edited file rather than trusting the first fix; corrected to the shorter, non-wrapping "overstates or understates," verified present. Two consecutive verification-anchor errors on the same sentence within one sub-project
- 2026-09-01 | Catch | Task 1 (research-skill-adoption) | The plan's Global Constraints forbade modifying any file outside `plugin/skills/` and `plugin/.claude-plugin/`, but the same plan's Task 1 Step 6 required drafting a README update whenever check_docs.py reports ACTION_NEEDED -- a direct self-contradiction between two sections of one plan, which the implementer hit and correctly escalated as DONE_WITH_CONCERNS rather than silently picking one. Neither writing-plans' Self-Review nor the spec's own review caught it: item 11 checks the header contains a Global Constraints section, and no item checks that section against the plan's own task steps for contradictions. Fixed the constraint in place to name `plugin/README.md`
- 2026-09-01 | Catch | Task 1 (research-skill-adoption) | check_docs.py returned ACTION_NEEDED against `plugin/README.md` for the second recorded time, independently rediscovered by a fresh implementer with no knowledge of the prior occurrence -- BUG-0001 (Open since 2026-08-28) already documents the exact root cause (line 47's bare-filename match never matching a nested path). The plan predicted the tool's output without checking the open bug tracker first, so a known-broken check got written into a plan step as though its result would be informative; recorded a second-occurrence note on BUG-0001 rather than filing a duplicate
- 2026-09-01 | Catch | Task 1 code quality review (research-skill-adoption) | The spec and plan both said "version bump in both manifests," but `plugin/.version-bump.json` declares SEVEN files carrying the plugin version -- `package.json`, four per-harness plugin.json files, `marketplace.json`, and `gemini-extension.json`. Five were left at 6.2.0. Neither the spec's Falsifiable Criterion 8 nor the plan's Task 1 caught it, because both were written from the two files the author happened to know about rather than from the repo's own declared manifest list; the declaring file was never read while writing either document
- 2026-09-01 | Catch | Task 1 code quality review (research-skill-adoption) | `plugin/scripts/bump-version.sh` exists specifically to prevent the version drift above, and its `--check` mode would have caught it -- but the script cannot run in this environment at all: `jq` is not installed (`which jq` returns nothing), so it dies at line 46 with `jq: command not found` before performing any check. The project's own drift-detection tooling has therefore never run on the maintainer's machine, which is why five-file drift could go unnoticed. The reviewer reported the script "prints DRIFT DETECTED"; verifying directly showed it never gets that far -- a reminder that a reviewer's claim about what a command outputs needs the same real-execution check as a plan's own predicted counts
- 2026-09-01 | Catch | Task 2 (research-skill-adoption) | Task 2 Step 4 predicted `grep -rn "REQUIRED SUB-SKILL" plugin/skills/ | grep -c "superfunk:"` would return 6, from "four pre-existing markers plus these two" -- the real baseline is 5, so the answer is 7. Two compounding errors: writing-plans/SKILL.md carries three such markers (lines 122, 295, 299), miscounted as two by reading a prior grep's output rather than counting it; and writing-skills/SKILL.md:283 is the house standard's own "✅ Good" example line, which matches any grep for the pattern it documents -- a self-matching-documentation case this project has not hit before. Caught by the implementer running the command rather than trusting the expected value, and reported as DONE_WITH_CONCERNS instead of silently accepting a mismatch. Third predicted-count error in this sub-project; the first two (`Propose 2-3 approaches` = 4 not 1, `What would lower it` = 2 not 1) were caught during plan-writing by running the commands, this one escaped to an implementer because the count was derived from earlier grep output already in context instead of a fresh run
- 2026-09-01 | Catch | Task 6 A/B trial (research-skill-adoption) | The A/B trial falsified two of the three requirements the spec added to brainstorming's step 4, exactly as Falsifiable Criterion 9 was written to allow. Confidence-grounding validated cleanly (absent in both pre-change arms, present in both post-change arms). The null option and the flip factor did not: the flip factor appeared unprompted in three of four arms, and in the scenario built to test the null option the PRE-change arm made "Measure before caching" its first approach and its recommendation ("180ms p50 may simply not be worth fixing"), while the post-change arm carrying the instruction offered three caching implementations and raised restraint only as a caveat. The unmodified skill beat the modified one on the exact dimension the change targeted
- 2026-09-01 | Catch | Task 6 trial design (research-skill-adoption) | Trial 1 could not have detected the null-option requirement at all: its prompt said "treat this as fully specified," which forecloses "build nothing" before the agent starts, so no candidate set could have contained a defer option. The first-pass read of that trial nearly recorded "requirement did not fire" as a property of the mechanism rather than of the scenario. Caught by asking what the prompt made possible before interpreting the result, and re-running with a scenario where deferring stayed defensible -- which then produced the real (and opposite-signed) finding. A negative trial result needs its scenario checked for whether it could have produced a positive one, the same way `ab-test-live-trials-for-behavior-change.md` Rule 2 checks that a prompt doesn't hand over its own answer
- 2026-09-01 | Catch | Task 2 (convention-retirement) | plan Task 2 Step 3 grep | The plan's verification anchor "carrying no attribution" spans a markdown line wrap in the very text the plan prescribes inserting -- "carrying no" ends one line, "attribution:" begins the next -- so a single-line grep returns 0 regardless of whether the edit landed correctly. The implementer inserted the prescribed text verbatim, ran the command, got 0, and reported DONE_WITH_CONCERNS rather than reflowing the prose to make the check pass, which would have hidden a plan defect behind a passing grep. Identical shape to the 2026-08-31 checkpoint-priority catch ("overstates or understates its own list"), and writing-plans' own item 10 did not prevent the recurrence: the anchor got chosen from the drafted text without ever being run against that text's actual line breaks. Corrected to "predates the attribution field", verified at 1. First notes.md entry written in the five-field format this same sub-project ships
- 2026-09-01 | Catch | Task 3 (convention-retirement) | plan Task 3 Step 4 grep | The plan predicted `grep -c "Zero-yield"` would return 2 ("one in Step 5's reason set, one in No Placeholders"); the real answer is 3, because the plan's own Step 2 insertion names Zero-yield twice -- once in the attribution-coverage sentence ("makes Zero-yield unusable") and once as the bolded reason item. The count was derived by scanning the drafted text for the bolded item and missing the earlier prose mention in the same block. Fifth predicted-count error this session and the fifth distinct sub-variety: bare substring over-matching, prose/code-block duplication, miscounted baseline plus a self-documenting example line, a phrase spanning a line wrap, and now undercounting occurrences inside the author's own drafted insertion. Every one of the last three was caught by an implementer running the command, never by plan self-review
- 2026-09-01 | Catch | Task 3 (convention-retirement) | none — found ad hoc | Across five predicted-count failures this session, the common root holds: writing-plans item 10 requires running verification commands against real content, and that got done -- but only against the file's PRE-edit state to establish baselines. A baseline run cannot detect a wrap, a duplicate, or an undercount that exists only in text not yet inserted. The gap is structural, not carelessness, and item 10's current wording does not close it. Candidate fix for a future spec: write the drafted insertion to a scratch file and run the intended grep against that file before finalizing the plan, which would have caught the line-wrap, prose-duplicate, and undercount failures alike
- 2026-09-01 | Catch | Task 5 trial run 1 (convention-retirement) | the trial itself | The trial fixture leaked the exact answer the trial existed to test for. The fixture build copied the project's real specs directory, which includes `2026-09-01-convention-retirement-design.md`, whose Context section names all three subsumption candidates the run was supposed to surface independently. The run detected the contamination while reading specs to establish Status flags, and disclosed it unprompted, explicitly citing the spec's own Falsifiable Criterion 8 against itself. Caught by the trial, not by its author, despite the plan carrying an explicit Global Constraint forbidding exactly this
- 2026-09-01 | Catch | Task 5 trial run 2 (convention-retirement) | the trial itself | The scrub failed a second time by a route the first fix did not consider: deleting the leaking spec from the fixture's working tree left run 1's committed review file in the fixture's git history, and run 2 retrieved it with `git show` while establishing its review window, since the file no longer existed on disk. It disclosed this too, unprompted. A trial fixture is a git repository; scrubbing its working tree does not scrub it
- 2026-09-01 | Catch | Task 5 trial (convention-retirement) | process-review Retirements section | All three subsumption candidates this spec's Context asserted got examined against shipped text and rejected, with citations since verified independently -- `notes.md` 2026-08-28 states the Self-Review "has no item checking a plan's own header against its own required template," which is why item 11 exists rather than duplicating items 1 and 2, and `notes.md` 2026-08-27 records item 8's grep scope as a known gap not reaching item 9. The claims were made from reading check titles and first sentences rather than from the record of what each check actually caught. Spec Context corrected; the new mechanism's first real act was falsifying its own justification
- 2026-09-01 | Catch | Process review (convention-retirement) | none — found ad hoc | The review's M1/M2/G2 stated that writing-plans Self-Review item 10 "exists to catch exactly this class and caught none of them" -- but item 10 never loaded. `~/.claude/plugins/config.json` reads `{"repositories": {}}`, so the session ran upstream superpowers 5.1.0 throughout, whose writing-plans carries 3 Self-Review items to superfunk's 14 and which ships no process-review skill at all. Superfunk's conventions got applied by hand, by the same agent that authored the documents under review, which is materially weaker evidence than a check firing in fresh context. The scope gap the findings identify belongs to the check's wording and survives, but the review claimed a mechanism failed when no mechanism had loaded. The trial findings stay exempt -- every --plugin-dir invocation pointed at the real plugin. Caught by the human partner asking which framework had actually been running, not by any check; the review had already been written and committed
- 2026-09-01 | Catch | Task 1 (taskq-trial-batch1-mechanical-fixes) | none — found ad hoc | The F1 verification grep for "already establishes them observably" returned 0 against a baseline-confirmed-correct edit: the phrase wraps across two lines in the live file (a `- skip if...) and\n  observably)` line break), the same line-wrap failure shape notes.md 2026-09-01 (Task 3, convention-retirement) already names as a structural gap in item 10's baseline-only verification. The shipped content was correct throughout; only the grep anchor needed shortening to a same-line substring ("already establishes them"). Re-confirms that entry's candidate fix (grep the actual drafted insertion, not a baseline prediction) — this plan predates that entry and didn't carry it forward
